<views:BasePreviewImagePage
    x:Class="MegaApp.Views.PreviewImagePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"    
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:triggers="using:WindowsStateTriggers"
    xmlns:views="using:MegaApp.Views"
    xmlns:viewModels="using:MegaApp.ViewModels"
    mc:Ignorable="d"
    d:DataContext="{d:DesignInstance viewModels:PreviewImageViewModel}">

    <Page.Resources>
        <DataTemplate x:Key="ImagePreviewTemplate">
            <Image
                Grid.Row="1"
                Margin="20,0,20,20"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Stretch="Uniform">
                <Image.Source>
                    <BitmapImage
                        UriSource="{Binding PreviewImageUri}"/>
                </Image.Source>
            </Image>
        </DataTemplate>
    </Page.Resources>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup>
                <VisualState x:Name="Desktop">
                    <VisualState.StateTriggers>
                        <triggers:DeviceFamilyStateTrigger DeviceFamily="Desktop"/>
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="PreviousButton.Visibility" Value="Collapsed" />
                        <Setter Target="NextButton.Visibility" Value="Collapsed" />
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="Mobile">
                    <VisualState.StateTriggers>
                        <triggers:DeviceFamilyStateTrigger DeviceFamily="Mobile"/>
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="PreviousButton.Visibility" Value="Visible" />
                        <Setter Target="NextButton.Visibility" Value="Visible" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
        
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock
            Grid.Row="0"
            Margin="12,20"
            VerticalAlignment="Center"
            Text="{Binding SelectedPreview.Name}"/>

        <FlipView
            x:Name="FlipView"
            Grid.Row="1"
            Margin="12,0"
            Background="Transparent"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            ItemTemplate="{StaticResource ImagePreviewTemplate}"
            ItemsSource="{Binding PreviewItems}"
            SelectedItem="{Binding SelectedPreview, Mode=TwoWay}"
            SelectionChanged="OnFlipViewSelectionChanged"/>

        <CommandBar
            Name="PageCommandBar"
            Grid.Row="2"
            Style="{StaticResource MegaCommandBarStyle}">            
            <AppBarButton
                Name="PreviousButton"
                Icon="Previous" 
                Label="{Binding PreviousText}" 
                Command="{Binding PreviousCommand}"/>
            <AppBarButton 
                Icon="Download" 
                Label="{Binding DownloadText}" 
                Command="{Binding DownloadCommand}"/>
            <AppBarButton 
                Icon="Link"
                Label="{Binding GetLinkText}" 
                Command="{Binding GetLinkCommand}"/>
            <AppBarButton
                Name="NextButton"
                Icon="Next" 
                Label="{Binding NextText}" 
                Command="{Binding NextCommand}"/>

            <CommandBar.SecondaryCommands>
                <AppBarButton
                    Icon="Rename" 
                    Label="{Binding RenameText}"
                    Command="{Binding RenameCommand}"/>
                <AppBarButton
                    Icon="Delete" 
                    Label="{Binding MoveToRubbishBinText}"
                    Command="{Binding MoveToRubbishBinCommand}"
                    Visibility="{Binding ParentFolder.CurrentViewState, ConverterParameter=CloudDrive, Converter={StaticResource FolderContentViewStateToVisibilityConverter}}"/>
                <AppBarButton
                    Icon="Delete" 
                    Label="{Binding RemoveText}"
                    Command="{Binding RemoveCommand}"
                    Visibility="{Binding ParentFolder.CurrentViewState, ConverterParameter=RubbishBin, Converter={StaticResource FolderContentViewStateToVisibilityConverter}}"/>
            </CommandBar.SecondaryCommands>
        </CommandBar>
    </Grid>
</views:BasePreviewImagePage>
